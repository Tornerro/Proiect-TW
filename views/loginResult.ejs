<!DOCTYPE html>
<html lang="en">

<head>
    <title>Login</title>
    <link href="/public/assets/stil.css" rel="stylesheet" type="text/css" />

    <script src="index.js"></script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width">
</head>

<body onload="markactive()">

    <%- include("../views/navbar.ejs") %>

    <% if(!locals.user){ %>
    <p style="text-align: center; font-size: 3em;">Login esuat!</p>

    <% } %>

    <% if(locals.user){ %>

    <p style="text-align: center; font-size: 3em;">Login reusit!

        <br>
        Salut, <%= user.username %>!
        <br>

        <% if(user.nrlogs==0){ %>
        Te-ai logat acum pentru prima data.
        <% } %>
        <% if(user.nrlogs>0){ %>
        Ultima oara te-ai logat de pe IP-ul <%= user.lastip %>, in ziua <%=user.dd %>.<%=user.mm %>.<%=user.yy %> la ora
        <%=user.h %>:<%=user.m %>:<%=user.s %>.
        <br>
        Te-ai logat de <%= user.nrlogs %> ori.
        <% } %>

    </p>

    <div class="user">
        <div> User:
            <%= user.username %>
        </div>
        <a href="/logout"><button>Logout</button></a>
    </div>

    <% } %>

</body>

</html>